# API TODO

백엔드 스펙 확정 후 작업한다.

```
src/api/
├─ axios.ts      # Axios Instance (baseURL, Authorization, Timeout, Interceptor, 공통 Error 처리)
├─ auth.ts       # 로그인 / 로그아웃
├─ user.ts       # 내 정보

```

- 페이지·컴포넌트에서 Axios를  nv.local.example` 참고.
