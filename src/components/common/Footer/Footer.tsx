export function Footer() {
  return (
    <footer className="h-12 flex items-center justify-center px-4 border-t border-border bg-surface text-xs text-secondary">
      © {new Date().getFullYear()} 정션
    </footer>
  );
}
